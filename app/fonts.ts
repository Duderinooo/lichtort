import { Inter } from 'next/font/google';
import { Sacramento } from 'next/font/google';
import { Rosarivo } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const rosaviro = Rosarivo({
  subsets: ['latin'],
  weight: '400',
});
